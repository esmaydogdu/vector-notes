import { createHash } from "crypto";

function vectorToColorHexHash(vector: number[]): string {
  // Convert vector to string and hash it
  const hash = createHash("md5").update(JSON.stringify(vector)).digest("hex");
  // Use first 6 hex digits for color
  return `#${hash.slice(0, 6)}`;
}